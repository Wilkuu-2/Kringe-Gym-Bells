using UnityEngine;

public class Trialscript : MonoBehaviour
{
    static float xd = 6.9f;
    // Start is called before the first frame update
    void Start()
    {
        var col = GetComponent<Collider>();       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
